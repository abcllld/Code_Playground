#include<stdio.h>

int main (){
     
     int num = 24;   //This will store a number 24 in the memory inside a box called num
     
     num = 28; //This will update i.e overwrite the previous data in the memory

     char star = '*';   //This will store a character * in the memory inside a box called star i.i star is the location of the memory

     return 0;
   
}

// num = variable

// int = data types

// return , main, int etc.... = keywords(reserved words)

// 28, 89, 24 == constants









