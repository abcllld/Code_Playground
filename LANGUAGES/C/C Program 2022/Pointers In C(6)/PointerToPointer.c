// Pointer to Pointer

// A variable that stores the memory
// address of another pointer

// Pointer to Pointer

// Syntax------------------>>>>>>>>>>>>>>>.

// int **pptr;
// char **pptr;
// float **pptr;

// NOTE(1):==========>It is not always necessary to write pptr or ptr ... we can write anything e.g *sth, *star....**ftr...........etc

// NOTE(2):==========>Pointer stores the address of a normal variable and PtoP stores the address of pointer variable

// NOTE(3):============> "*" is used when we require value AND "&" is used to get the address

