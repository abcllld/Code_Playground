//  Return Keyword
// When JavaScript reaches a return statement,
// the function will stop executing.
// Functions often compute a return value.
// The return value is "returned" back to the "caller"



function sum(a,b){
    var total= a+b;             //this will change
    console.log(total);         //this will also change
    }
    
    var funEx = sum(20,87);



function sum(a,b){
return total = a+b;

}

var funEx = sum(288,87);
console.log('The sum of two no is ' + funEx);

// This is the use return keyword